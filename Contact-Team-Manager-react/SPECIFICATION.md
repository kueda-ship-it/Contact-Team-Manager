# プロジェクト仕様書: Contact Team Manager

## プロジェクト概要
連絡先とチーム管理を行うためのウェブアプリケーションです。React と Vite を使用して構築されています。

## 技術スタック
- **Frontend**: React (v19), TypeScript
- **Build Tool**: Vite (v7)
- **CSS**: Vanilla CSS (PostCSS)
- **Backend/Services**: Supabase, Microsoft Graph API (MSAL)

## 対応プラットフォーム
- **Windows**: 確認済み
- **Apple Silicon (macOS)**: 対応（Vite/Rollup の動的バイナリ選択による）
- **モバイルデバイス / レスポンシブ対応**: スマートフォンやタブレットでも利用可能なレスポンシブデザインに対応（画面幅768px以下でモバイルUIに切り替わります）。PWAによるインストールもサポート。

## 主な機能
- **フィード/スレッド**: チームごとの連絡事項の投稿、返信、ステータス管理、メンション機能（@ユーザー/@タグ）。
  - **URL自動リンクとプレビュー**: 投稿内のURLを自動的にリンク化し、Supabase Edge Function (`get-link-preview`) を利用してOGP情報（タイトル、説明、画像）を動的に取得・表示。
  - **画像アップロード即時プレビューと詳細確認**: 
    - 投稿・返信時にファイルを選択または画像を貼り付けた際、アップロード完了を待たずに即座にプレビューを表示。
    - プレビューサイズを拡大（最大 200x150）し、クリックでフルサイズ確認が可能。
  - **高精度レンダリング**: CSS の `image-rendering` 最適化および OneDrive からの 1600px 高解像度サムネイル取得により、鮮明な画像表示を実現。
- **ダッシュボード**: 
  - 投稿数、完了数、解決時間の基本統計。
  - 返信率、総返信数、返信数分布の可視化。
  - 返信の有無による解決スピードの比較分析。
  - 長時間スレッドランキング（Top 100）およびスレッドへのダイレクトナビゲーション。
- **ユーザー管理（ホワイトリスト）**: 管理者による事前メールアドレス登録制。初回ログイン時に自動的にプロフィールを生成。
- **ナレッジ共有**: 内部ナレッジ機能（開発中）および外部リンク。

## 開発環境のセットアップ

### システム要件
- Node.js (最新の LTS 推奨)
- npm

### 開発サーバーの起動方法
1. 依存関係のインストール:
   ```bash
   npm install
   ```
2. 開発サーバーの起動:
   ```bash
   npm run dev
   ```
   デフォルトで `http://localhost:5173` で起動します。

### ビルド
```bash
npm run build
```
